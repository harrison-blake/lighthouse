## Lighthouse
A go exectuable that takes content, in markdown format, injects it into html templates to build a staic site. 
Currently be used to build my portfolio [here](https//www.harrisonblake.net).

## Development
```
go install .
lighthouse build
or
lighthouse build --serve (for localhost:8080 access)
```

D#
